SMTP_ADDRESS: 'smtp.gmail.com'
SMTP_PORT: 587
SMTP_DOMAIN: 'gmail.com'
SMTP_USER_NAME: 'username@change.me'
SMTP_PASSWORD: '<password>'
SMTP_AUTHENTICATION: 'plain'
SMTP_ENABLE_STARTTLS_AUTO: True

APP_HOST: 'public.domain.com'
APP_PROTOCOL: 'http'
EMAIL_ADDRESS: 'noreply@change.me'

SECRET_KEY_BASE: 'REPLACE with rake secret output'

NEW_RELIC_LICENSE_KEY: 'REPLACE with the one provided by NewRelic'

HIGHEST_SCORE_ORGANIZATIONS: '<%= {
  '1' => '31-01-2014',
  '2' => '31-02-2014'
}.to_json %>'

production:
  APP_PROTOCOL: 'https'
