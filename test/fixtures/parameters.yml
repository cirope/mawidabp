<% DEFAULT_PARAMETERS.each do |name, value| %>
parameter_<%= name %>:
  name: <%= name %>
  description: Parameter <%= name %>
  organization: default_organization
  value: "<%= value.kind_of?(Array) ? value.to_yaml.gsub(/\n/,"\\n").gsub(/"/, '\\"') : value %>"
<% end %>