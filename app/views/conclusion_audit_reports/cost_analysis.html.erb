<%= render :partial => 'shared/filter_form',
  :locals => {:action => :cost_analysis, :extra_params =>
    {:include_details => params[:include_details]},
  :forms => {:date_range => true}} %>

<% @periods.each do |period| %>
  <h1 class="simple_title with_border_bottom">
    <strong><%= "#{Period.model_name.human}: #{period.inspect}" %></strong>
  </h1>
  <% unless @total_cost_data[period].blank? %>
    <table class="table table-condensed table-striped">
      <thead>
        <tr>
          <% @column_order.each do |col_name, col_width| %>
            <th>
              <%= t :"conclusion_audit_report.cost_analysis.general_column_#{col_name}" %>
            </th>
          <% end %>
        </tr>
      </thead>
      <tbody>
        <% @total_cost_data[period].each do |total_data| %>
          <tr>
            <% total_data.each do |col_data| %>
              <td><%= raw col_data %></td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p style="margin: 1em;">
      <em><%= t 'conclusion_audit_report.cost_analysis.without_audits_in_the_period' %></em>
    </p>
  <% end %>

  <% unless @detailed_data[period].blank? %>
    <% @detailed_data[period].each do |detailed_data| %>
      <h2 style="margin: 1em 0em;"><%= detailed_data[:review] %></h2>
      <table class="table table-condensed table-striped">
        <thead>
          <tr>
            <% @detailed_column_order.each do |col_name, col_width| %>
              <th>
                <%= t :"conclusion_audit_report.cost_analysis.detailed_column_#{col_name}" %>
              </th>
            <% end %>
          </tr>
        </thead>
        <tbody>
          <% detailed_data[:data].each do |data| %>
            <tr>
              <% data.each do |col_data| %>
                <td><%= raw col_data %></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>
  <% end %>
<% end %>

<div id="links">
  <%= link_to t('label.back'), :action => :index %> |
  <%= link_to t('label.download'), '#', :onclick =>
    "Helper.showItem('#customize_report_form'); return false;" %>
</div>
<div id="customize_report_form" style="display: none;">
  <div>
  <%= render :partial => 'shared/customize_report_form', :locals => {
    :options => {
      :form_name => 'report',
      :url => request.query_parameters.merge({
          :action => :create_cost_analysis,
          :include_details => params[:include_details],
          :_ts => Time.now.to_i
        }),
      :fields => [
        {
          :name => :report_title,
          :label => t('customize_report_form.title'),
          :value => t(params[:include_details].blank? ?
              'conclusion_audit_report.cost_analysis.general_title' :
              'conclusion_audit_report.cost_analysis.detailed_title')
        }
      ]
    }
  } %>
  </div>
</div>
