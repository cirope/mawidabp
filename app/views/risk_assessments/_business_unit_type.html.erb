<% id = f.object.object_id %>
<fieldset class="business_unit_type">
  <div class="form-inputs">
    <div class="row">
      <div class="col-md-11">
        <%= f.input :id, as: :string, label: false,
          input_html: {
            id: "business_unit_type_#{id}",
            name: 'business_unit_type[business_unit_type_data]',
            title: t('label.search'),
            autofocus: true,
            data: {
              disabled_on_apply: true,
              enable_add_risk_assessment_items: true,
              autocomplete_url: auto_complete_for_business_unit_type_risk_assessments_path,
              autocomplete_id_target: "#hidden_business_unit_type_#{id}"
            }
          } %>
          <%= f.input "hidden_business_unit_type_#{id}", as: :hidden, input_html: {
            value: f.object.id,
            id: "hidden_business_unit_type_#{id}",
            data: { apply_id: true }
          } %>
      </div>
      <div class="col-md-1">
        <div class="toggle-visibility">
          <%= link_to_remove_nested_item f %>
        </div>
      </div>
    </div>
  </div>
</fieldset>
