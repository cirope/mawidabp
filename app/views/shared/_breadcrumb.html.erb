<nav class="mb-4">
  <ol class="breadcrumb">
    <% crumbs.each_with_index do |crumb, i| %>
      <li class="breadcrumb-item<%= ' active' if i == crumbs.size - 1  %>">
        <%= crumb %>
      </li>
    <% end %>
  </ol>
</nav>
