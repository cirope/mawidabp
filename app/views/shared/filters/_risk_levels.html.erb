<div id="risk_levels_filter" class="filter" <%= 'style="display: none;"' unless display %>>
  <div class="row">
    <div class="col-md-6">
      <div class="form-group">
        <% selected = params[action] ? params[action][:risk] : nil %>
        <%= f.label :risk, Weakness.human_attribute_name(:risk), class: 'col-md-3' %>
        <div class="col-md-9">
          <%= risk_field(f, { label: false, selected: selected }) %>
        </div>
      </div>
    </div>
  </div>
</div>
