<ul class="nav navbar-nav navbar-right">
  <li>
    <a href="/help" target="_blank" title="<%= t 'menu.help_title' %>">
      <span class="glyphicon glyphicon-question-sign"></span>
    </a>
  </li>

  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
      <span class="glyphicon glyphicon-user"></span>
      <span class="caret"></span>
    </a>

    <ul class="dropdown-menu">
      <% if current_organization && current_organization.ldap_config.blank? %>
        <li>
          <%= link_to t('users.profiles.edit.title'), edit_users_profile_path(user) %>
        </li>
        <li class="divider"></li>
      <% end %>
      <li>
        <%= link_to t('navigation.logout'), logout_path, data: { method: :delete } %>
      </li>
    </ul>
  </li>
</ul>

<%= render 'shared/time_left' %>
