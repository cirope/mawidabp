<%= render('shared/downloads',
  right: true,
  links: [
    link_to(
      t('label.download'), {
        :action => :export_to_pdf, :id => @conclusion_final_review, :_ts => Time.now.to_i
      }, :class => 'dropdown-item'
    ),
    link_to(
      t('conclusion_final_review.download_rtf'), {
        :action => :export_to_rtf, :id => @conclusion_final_review, :_ts => Time.now.to_i, :format => :rtf
      }, :class => 'dropdown-item'
    ),
    link_to(
      t('conclusion_final_review.download_brief'), {
        :action => :export_to_pdf, :id => @conclusion_final_review, :export_options => {:brief => '1'}, :_ts => Time.now.to_i
      }, :class => 'dropdown-item'
    )
  ].compact
) %>
