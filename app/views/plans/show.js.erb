+function () {
  $('#planned-items').
    html('<%= j render_business_unit_type_plan_items %>').
    find('.card').
    effect('highlight')

  setTimeout(function () { location.hash = 'planned-items' })
}()
